# Core
torch>=2.1.0
torchvision>=0.16.0
numpy<2.0
Pillow>=10.0

# 3D / mesh
trimesh>=4.0.0
open3d>=0.18.0
scipy>=1.11.0
pyrender>=0.1.45

# Metrics
lpips>=0.1.4

# Background removal
rembg>=2.0.50
onnxruntime

# Web / API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
gradio>=4.20.0

# Model hub
huggingface-hub>=0.21.0
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0

# Data
pandas>=2.0.0
pydantic>=2.0.0

# TripoSR  (install separately — not on PyPI)
# pip install git+https://github.com/VAST-AI-Research/TripoSR.git

# TRELLIS  (install separately — not on PyPI)
# pip install git+https://github.com/microsoft/TRELLIS.git

# LGM  (install separately — not on PyPI)
# pip install git+https://github.com/3DTopia/LGM.git kiui
